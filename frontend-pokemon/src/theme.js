import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  colors: {
    primary: "#3363b1",
    secondary: "#ffcb05",
    white: "white",
    atk: "#ff5733",
    def: "#00b300",
    sta: "#ffd700",
  },
});

export default theme;
